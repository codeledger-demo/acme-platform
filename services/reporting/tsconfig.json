// Generated by marcus-webb at 2026-04-08T05:42:02.391Z
/**
 * arc1-006 — Configuration & Types
 *
 * Shared configuration, constants, and type definitions
 * for the usage-reporting-pipeline module.
 *
 * @author Marcus Webb
 * @date 2026-04-08T05:42:02.391Z
 */

/** Environment-aware configuration for usage-reporting-pipeline */
export interface Config {
  /** Whether the feature is enabled (default: true) */
  enabled: boolean;

  /** Base URL for external API calls */
  baseUrl: string;

  /** Request timeout in milliseconds (default: 10000) */
  timeoutMs: number;

  /** Maximum retry attempts for transient failures (default: 3) */
  maxRetries: number;

  /** Log level for this module */
  logLevel: 'debug' | 'info' | 'warn' | 'error';
}

/** Default configuration values */
export const DEFAULT_CONFIG: Config = {
  enabled: true,
  baseUrl: process.env._BASE_URL ?? 'http://localhost:3000',
  timeoutMs: 10_000,
  maxRetries: 3,
  logLevel: 'info',
};

/** Status codes used by the usage-reporting-pipeline module */
export const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  PENDING: 'pending',
  ERROR: 'error',
} as const;

export type Status = (typeof STATUS)[keyof typeof STATUS];

/** Feature flag keys for usage-reporting-pipeline */
export const FEATURE_FLAGS = {
  ENABLE_V2: 'usage-reporting-pipeline.enable_v2',
  ENABLE_METRICS: 'usage-reporting-pipeline.enable_metrics',
  ENABLE_CACHING: 'usage-reporting-pipeline.enable_caching',
} as const;

/**
 * Load configuration from environment variables with defaults.
 */
export function loadConfig(overrides: Partial<Config> = {}): Config {
  return {
    ...DEFAULT_CONFIG,
    ...overrides,
  };
}
