// Generated by sara-chen at 2026-04-08T05:29:53.102Z
/**
 * arc1-002 — Configuration & Types
 *
 * Shared configuration, constants, and type definitions
 * for the auth-service-foundation module.
 *
 * @author Sara Chen
 * @date 2026-04-08T05:29:53.102Z
 */

/** Environment-aware configuration for auth-service-foundation */
export interface Config {
  /** Whether the feature is enabled (default: true) */
  enabled: boolean;

  /** Base URL for external API calls */
  baseUrl: string;

  /** Request timeout in milliseconds (default: 10000) */
  timeoutMs: number;

  /** Maximum retry attempts for transient failures (default: 3) */
  maxRetries: number;

  /** Log level for this module */
  logLevel: 'debug' | 'info' | 'warn' | 'error';
}

/** Default configuration values */
export const DEFAULT_CONFIG: Config = {
  enabled: true,
  baseUrl: process.env._BASE_URL ?? 'http://localhost:3000',
  timeoutMs: 10_000,
  maxRetries: 3,
  logLevel: 'info',
};

/** Status codes used by the auth-service-foundation module */
export const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  PENDING: 'pending',
  ERROR: 'error',
} as const;

export type Status = (typeof STATUS)[keyof typeof STATUS];

/** Feature flag keys for auth-service-foundation */
export const FEATURE_FLAGS = {
  ENABLE_V2: 'auth-service-foundation.enable_v2',
  ENABLE_METRICS: 'auth-service-foundation.enable_metrics',
  ENABLE_CACHING: 'auth-service-foundation.enable_caching',
} as const;

/**
 * Load configuration from environment variables with defaults.
 */
export function loadConfig(overrides: Partial<Config> = {}): Config {
  return {
    ...DEFAULT_CONFIG,
    ...overrides,
  };
}
