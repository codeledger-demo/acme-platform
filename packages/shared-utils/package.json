// Generated by sara-chen at 2026-04-08T05:23:48.047Z
/**
 * arc1-001 — Configuration & Types
 *
 * Shared configuration, constants, and type definitions
 * for the project-setup-&amp;-ci-configuration module.
 *
 * @author Sara Chen
 * @date 2026-04-08T05:23:48.047Z
 */

/** Environment-aware configuration for project-setup-&amp;-ci-configuration */
export interface Config {
  /** Whether the feature is enabled (default: true) */
  enabled: boolean;

  /** Base URL for external API calls */
  baseUrl: string;

  /** Request timeout in milliseconds (default: 10000) */
  timeoutMs: number;

  /** Maximum retry attempts for transient failures (default: 3) */
  maxRetries: number;

  /** Log level for this module */
  logLevel: 'debug' | 'info' | 'warn' | 'error';
}

/** Default configuration values */
export const DEFAULT_CONFIG: Config = {
  enabled: true,
  baseUrl: process.env._BASE_URL ?? 'http://localhost:3000',
  timeoutMs: 10_000,
  maxRetries: 3,
  logLevel: 'info',
};

/** Status codes used by the project-setup-&amp;-ci-configuration module */
export const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  PENDING: 'pending',
  ERROR: 'error',
} as const;

export type Status = (typeof STATUS)[keyof typeof STATUS];

/** Feature flag keys for project-setup-&amp;-ci-configuration */
export const FEATURE_FLAGS = {
  ENABLE_V2: 'project-setup-&amp;-ci-configuration.enable_v2',
  ENABLE_METRICS: 'project-setup-&amp;-ci-configuration.enable_metrics',
  ENABLE_CACHING: 'project-setup-&amp;-ci-configuration.enable_caching',
} as const;

/**
 * Load configuration from environment variables with defaults.
 */
export function loadConfig(overrides: Partial<Config> = {}): Config {
  return {
    ...DEFAULT_CONFIG,
    ...overrides,
  };
}
