apiVersion: v1
kind: Service
metadata:
  name: acme-platform
  labels:
    app: acme-platform
spec:
  type: ClusterIP
  selector:
    app: acme-platform
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
